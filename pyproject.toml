[project]
name = "fde-learning"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "openai>=3.11.0",
    "python-dotenv>=1.2.3",
]

[project.scripts]
fde-learning = "fde_learning:main"

[build-system]
requires = ["uv_build>=0.12.3,<0.13.0"]
build-backend = "uv_build"
